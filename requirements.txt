# GeoSight Aurora — Pixxels Edition
# Core requirements

streamlit>=1.32.0
numpy>=1.24.0
matplotlib>=3.7.0
pandas>=2.0.0
Pillow>=10.0.0

# Geospatial (optional but strongly recommended for real satellite image upload)
# rasterio enables full GeoTIFF multi-band reading from real satellite images
rasterio>=1.3.0

# Interactive maps (optional)
folium>=0.14.0

# Spatial smoothing (optional, falls back to manual implementation if absent)
scipy>=1.10.0